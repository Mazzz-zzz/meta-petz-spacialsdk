Current Status & Plan

  Where we're at:

  1. Goal: Add physics colliders at room boundaries so thrown bones bounce off walls
  2. MRUK Issue: Getting XrTime conversion error: -7 when loading scene data - OpenXR timing problem
  3. Debug Issue: Box components alone don't render visibly (need a Mesh/GLB)
  4. Current Build (v2):
    - Disabled MRUK scene loading (commented out)
    - Disabled OVRMetrics spam logs
    - Added a debug bone model at (0, 1, -1.5) to verify rendering works
    - Text shows "(v2)" to confirm code is updated

  Plan:

  1. First - Confirm v2 build shows the debug bone floating in front of you
  2. Then - Add simple hardcoded wall colliders at fixed positions (e.g., 3m in each direction) using just
  Physics component - skip MRUK for now
  3. Test - Throw the bone and see if it bounces off the hardcoded walls
  4. Later - Once physics collisions work, re-enable MRUK to get actual room boundaries

  This approach isolates whether the issue is:
  - MRUK not loading room data (the XrTime error)
  - Physics colliders not working at all
  - Something else

  Next step: Close and relaunch the app to check if you see the debug bone!